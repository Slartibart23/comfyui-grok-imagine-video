[project]
name = "comfyui-grok-imagine"
description = "ComfyUI custom nodes for the official xAI Grok Imagine API: image generation, multi-image editing (up to 3 sources), video generation with async polling, and saving videos to any path."
version = "1.0.2"
license = { file = "LICENSE" }
dependencies = ["requests>=2.28", "numpy>=1.24", "Pillow>=9.0", "imageio>=2.31", "imageio-ffmpeg>=0.4.9"]

[project.urls]
Repository = "https://github.com/Slartibart23/comfyui-grok-imagine"

[tool.comfy]
DisplayName = "Grok Imagine (xAI)"
PublisherId = ""
